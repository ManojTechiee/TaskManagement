import { StyleSheet} from 'react-native';


const styles = StyleSheet.create({
    container: {
      backgroundColor: '#fff',
      flex:1
    },
    noDataText: {
      fontSize: 12,
      textAlign: 'center',
      marginTop: 20,
    },
  });


export default styles;